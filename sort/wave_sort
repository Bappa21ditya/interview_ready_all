vector<int> Solution::wave(vector<int> &A) {
    sort(A.begin(),A.end());
     int n=A.size();
    // vector<int>s1;
    // vector<int>r2;
    // int j=0;
    for(int i=0;i<n-1;i=i+2)
    {
        
        swap(A[i],A[i+1]);
    
    }
    return A;
}
